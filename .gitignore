# Virtual environments
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local model weights (too large for GitHub — keep these on disk only)
*.gguf
models/.cache/

# Local data / runtime artifacts
chroma_db/
logs/
*.log
vault/
# Documents the user uploaded through the API, alongside the vault: their own
# data, not project source, and indexed into chroma_db/ anyway.
uploads/

# Secrets
.env
.env.local
